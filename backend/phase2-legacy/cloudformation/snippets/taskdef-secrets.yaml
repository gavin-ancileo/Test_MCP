# Snippet: add to ContainerDefinitions[].Secrets in your TaskDefinition
# Replace <ACCOUNT_ID> and region if using Secrets Manager ARNs directly.
Secrets:
  - Name: DATABASE_URL
    ValueFrom: /aap/app/DATABASE_URL
  - Name: REDIS_URL
    ValueFrom: /aap/app/REDIS_URL
# If using Secrets Manager for Google SA JSON later:
#  - Name: GOOGLE_SERVICE_ACCOUNT_JSON
#    ValueFrom: arn:aws:secretsmanager:ap-southeast-1:<ACCOUNT_ID>:secret:/aap/app/GOOGLE_SERVICE_ACCOUNT_JSON
