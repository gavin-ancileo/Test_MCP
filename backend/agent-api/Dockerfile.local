FROM python:3.11-slim
WORKDIR /app
COPY requirements.txt .
RUN pip install --no-cache-dir -r requirements.txt
COPY . .
EXPOSE 8000
# Use app.py with MCP tools fixes for local testing
CMD ["uvicorn", "app:app", "--host", "0.0.0.0", "--port", "8000"]
