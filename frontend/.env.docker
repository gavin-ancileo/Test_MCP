  # =====================================
  # Docker Environment Configuration
  # Copy this to .env and update values
  # =====================================
  
  # AWS Cognito Configuration (Production)
  VITE_COGNITO_DOMAIN=https://aap-uat-20251014.auth.ap-southeast-2.amazoncognito.com
  VITE_COGNITO_CLIENT_ID=20bsno0fmojtchqem9vacrlivt
  VITE_COGNITO_USER_POOL_ID=ap-southeast-2_TFmPkO0Lc
  VITE_COGNITO_REGION=ap-southeast-2
  
  # ⚠️ IMPORTANT: Update this with your actual domain
  VITE_COGNITO_REDIRECT_URI=https://your-domain.com/callback
  
  # Backend API Configuration
  VITE_AGENTCORE_URL=http://aap-alb-ALB-BD6uDqhT1dNT-61728180.ap-southeast-2.elb.amazonaws.com/agentcore
+ VITE_API_BASE_URL=http://aap-alb-ALB-BD6uDqhT1dNT-61728180.ap-southeast-2.elb.amazonaws.com/agentcore
  
  # Integration Features
  VITE_ENABLE_GITHUB_INTEGRATION=true
  VITE_ENABLE_JIRA_INTEGRATION=true
  
  # Environment
  VITE_ENVIRONMENT=production
  
  # =====================================
  # DEPLOYMENT CHECKLIST
  # =====================================
  # 
  # ✅ Update VITE_COGNITO_REDIRECT_URI with actual domain
  # ✅ Verify backend URL is correct
  # ✅ Add redirect URI to Cognito console
  # ✅ Test health check endpoint
  # ✅ Configure SSL/TLS for production
  # 
  # =====================================